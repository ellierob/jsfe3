//get center Element
const divs = document.querySelectorAll('div');
divs.forEach((d) => {
    const cents = divs.querySelectorAll('.cent')
});
const cents = div.querySelectorAll('cent');

// get center element height

//get middle column
const mid = document.querySelectorAll('cent');
//get middle column height

// get pos from 1/2(MCH-CEH)

//get middle column element ending closest to pos
